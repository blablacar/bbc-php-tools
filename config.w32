// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("memtools", "for memtools support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("memtools", "enable memtools support", "no");

if (PHP_MEMTOOLS != "no") {
	EXTENSION("memtools", "memtools.c");
}

